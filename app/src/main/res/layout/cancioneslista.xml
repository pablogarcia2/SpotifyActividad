<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black"
    tools:context=".BuscarFragment">

    <ImageView
        android:id="@+id/volverAtras"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/baseline_arrow_back_24"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:padding="16dp"/>

    <TextView
        android:id="@+id/nombrePlaylist"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="18sp"
        android:textColor="@color/white"
        android:layout_marginTop="15dp"
        tools:text="Nombre de la Playlist"
        android:layout_toEndOf="@id/volverAtras"
        android:layout_marginStart="8dp"
        android:layout_alignBaseline="@id/volverAtras" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycleCanciones"
        android:layout_marginTop="30dp"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/volverAtras"
        android:background="@color/black" />

</RelativeLayout>

